name: GitHub invite

on:
  issues:
    types: [labeled]

jobs:
  automate_invite:
    runs-on: ubuntu-latest
    steps:
      - name: Invite on label
        uses: vj-abigo/invite-on-label@v1.4
        with:
          organization: cfmlc
          label: invite me to the organization
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          comment: 'Invitation sent to join the CFML Community GitHub Organization. Welcome to the community 🎉'
        env:
          INVITE_TOKEN: ${{ secrets.INVITE_TOKEN }}
